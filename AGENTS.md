# Mercado Artesanal Tesis

## Frontend Workflow

- Use `.opencode/skills/stitch-ui-extractor/SKILL.md` when converting Stitch-generated HTML and rendered screenshots from `ReferenciasUI/` into React components.
- Preserve the modular structure under `src/modules/` and keep shared UI in `src/componentes/` only when it is reusable across roles.
- Current priority is hardcoded visual screens for the thesis report, not backend integration.

## Version Control Workflow

- Use `/home/life4death/.config/opencode/skills/branch-pr/SKILL.md` when creating branches, preparing PRs, or opening PRs for review.
- Use `/home/life4death/.config/opencode/skills/comment-writer/SKILL.md` when writing PR comments, issue replies, reviews, or collaboration updates.
- Branch names should follow `branch-pr` conventions: `type/description`, lowercase, no spaces.
