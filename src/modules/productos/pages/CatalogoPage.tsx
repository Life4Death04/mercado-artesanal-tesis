import { PendingDesignPage } from '../../../componentes/layout/PendingDesignPage'

export function CatalogoPage() {
  return (
    <PendingDesignPage
      actor="Consumidor"
      title="Catálogo pendiente de diseño"
      description="Esta ruta queda reservada hasta recibir la referencia Stitch correspondiente."
    />
  )
}
