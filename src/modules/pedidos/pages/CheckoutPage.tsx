import { PendingDesignPage } from '../../../componentes/layout/PendingDesignPage'

export function CheckoutPage() {
  return (
    <PendingDesignPage
      actor="Consumidor"
      title="Checkout pendiente de diseño"
      description="Ruta reservada para la referencia Stitch de confirmación de compra."
    />
  )
}
