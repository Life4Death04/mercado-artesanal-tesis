import { PendingDesignPage } from '../../../componentes/layout/PendingDesignPage'

export function CarritoPage() {
  return (
    <PendingDesignPage
      actor="Consumidor"
      title="Carrito pendiente de diseño"
      description="Ruta reservada para la referencia Stitch del carrito de compras."
    />
  )
}
