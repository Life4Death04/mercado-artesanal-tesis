# Stripe client configuration
# Set this locally to a Stripe publishable key (pk_...). Never put a secret key
# (sk_...) in Vite environment variables or any client-side source file.
VITE_STRIPE_PUBLISHABLE_KEY=
